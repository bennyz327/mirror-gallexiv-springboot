server:
  port: 8080
#  servlet:
#    context-path: /gallexiv
#    https://子網域名.網域名.主域/context-path(/功能、系統名)
spring:
  jackson:
    serialization:
      fail-on-empty-beans: false
  jpa:
    database: mysql
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        show_sql: true
        format_sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/gallexiv_v3?useSSL=false&serverTimezone=UTC
    username: benny
    password: benben327327
    driver-class-name: com.mysql.cj.jdbc.Driver
  mvc:
    hiddenmethod:
      filter:
        enabled: true
    view:
      prefix:
      suffix:
  security:
    user:
      name: admin
      password: admin1234
  data:
    redis:
      jedis:
        pool:
          enabled: true
          max-active: 8
          max-idle: 8
          max-wait: 1
          min-idle: 0
      host: 127.0.0.1
      port: 6379
      database: 1
      connect-timeout: 10000
      password: benben327327
springdoc:
  swagger-ui:
    path: /api/doc

gallexiv:
  # 網域用,分隔
  allowedOrigins: "http://localhost:3100"
  allowedMethods: "GET,POST,PUT,DELETE,OPTIONS"